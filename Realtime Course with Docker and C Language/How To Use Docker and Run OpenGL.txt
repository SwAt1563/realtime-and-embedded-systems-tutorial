# to run the project 
# go to the project directory by using (cd) 
# write in console
# docker-compose -f docker-compose.yml up  --build -d
# then open the docker desktop application and copy the container id
# then open git console and write
# winpty docker exec -it container_id bash
# to open console

# you should download VNC server and viewer-client
# the password on VNC Client(Viewer) is: headless
# For run the OpenGL on VNC